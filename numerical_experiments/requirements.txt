# Extra dependencies for the numerical_experiments/generate_data/ scripts.
# Also installable as the `numexp` extra (`pip install -e .[numexp]`).
#
# Needed ONLY by the gravitational-wave scripts (probe_gw.py,
# hessian_spectrum.py, coupling_mi.py). Tested combination: bilby 2.8.0 /
# lalsuite 7.26.13.
bilby==2.8.0
lalsuite==7.26.13

# Note: the GW panels of Figure 1 aren't reproducible bit-for-bit even with
# these versions -- see numerical_experiments/README.md.
